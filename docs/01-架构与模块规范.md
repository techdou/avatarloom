# 架构与 Block 模块规范

> **已合并到 `docs/block-development.md`**（2026-08-09）。
>
> 本文的原内容（Block 生命周期、运行方式、Manifest、核心约束）已完整合并进 `docs/block-development.md` 末尾"核心约束"章节，并补充了 review 后新增的约束（实例属性隔离、路径遍历校验）。
>
> 请直接阅读 `docs/block-development.md`。
