# 架构与 Block 模块规范

## Block 生命周期

```python
class Block:
    async def setup(self, context, config): ...
    async def warmup(self): ...
    async def process(self, event): ...
    async def reset(self, session_id): ...
    async def health(self): ...
    async def shutdown(self): ...
```

```python
class StreamingBlock(Block):
    async def open_stream(self, session): ...
    async def push(self, chunk): ...
    async def flush(self): ...
    async def close_stream(self): ...
```

## 运行方式

- `python_inproc`
- `python_process`
- `http_remote`
- `websocket_remote`
- `grpc_remote`
- `mock`

## Block Manifest

必须声明：标识、版本、分类、入口、输入输出事件、能力、资源要求、配置 Schema、健康检查和安装 Extra。

## 核心约束

1. Adapter 负责模型差异。
2. Runtime 只依赖能力和事件协议。
3. Block 必须可独立测试。
4. Block 必须支持超时和取消。
5. Block 必须报告健康状态。
6. 重型依赖使用 Optional Extra 或独立环境。
7. 浏览器不能绕过 Runtime Gateway 直连模型。
